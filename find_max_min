#include <stdio.h>

int main()
{
    //FIND MAX value from 3 values
    
    int a, b, c, max;
    printf("Input first value: ");
    scanf("%d", &a);
    printf("Input second value: ");
    scanf("%d", &b);
    printf("Input third value: ");
    scanf("%d", &c);
    
    if(a >= b){
        if(a >= c){
            printf("Maximum value of three values: %d\n", a);
        }
        else if (c >= a){
            printf("Maximum value of three values: %d\n", c);
        }
        else{
            
        }
    }
    else if(b >= a){
        if(b >= c){
            printf("Maximum value of three values: %d\n", b);
        }
        else if (c >= b){
            printf("Maximum value of three values: %d\n", c);
        }
        else{
            
        }
}
    else{
        
        }
        
   printf("-------------------------------------------\n");     
    //FIND MIN value from 3 values
    
    int min;
    printf("Input first value: ");
    scanf("%d", &a);
    printf("Input second value: ");
    scanf("%d", &b);
    printf("Input third value: ");
    scanf("%d", &c);
    
    if(a >= b){
        if(b >= c){
            printf("Minimum value of three values: %d\n", c);
        }
        else if(c >= b){
            printf("Minimum value of three values: %d\n", b);
        }
        else{
            
        }
    }
    
    else if(b >= a){
        if(a >= c){
            printf("Minimum value of three values: %d\n", c);
        }
        else if(c >= a){
            printf("Minimum value of three values: %d\n", a);
        }
        else{
            
        }
    }
    else{
        
    }
        
}    
    
