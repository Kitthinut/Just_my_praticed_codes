#include <stdio.h>

int main()
{
    int a, b, c, max;
    printf("Input first value: ");
    scanf("%d", &a);
    printf("Input second value: ");
    scanf("%d", &b);
    printf("Input third value: ");
    scanf("%d", &c);
    
    if(a >= b){
        if(a >= c){
            printf("Maximum value of three values: %d", a);
        }
        else if (c >= a){
            printf("Maximum value of three values: %d", c);
        }
        else{
            
        }
    }
    else if(b >= a){
        if(b >= c){
            printf("Maximum value of three values: %d", b);
        }
        else if (c >= b){
            printf("Maximum value of three values: %d", c);
        }
        else{
            
        }
}
    else{
        
        }
    
}
